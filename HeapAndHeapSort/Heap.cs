namespace HeapAndHeapSort
{
    /// <summary>
    /// Special case of binary tree.
    /// Binary tree should be complete (every node)
    /// Max Heap every parent should be greater or equal to children
    /// Min Heap every parent should be lesser or equal to children
    /// Heaps are implemented as arrays
    /// Heapifying the process of converting a binary tree into a heap
    /// </summary>
    public class Heap
    {
    }
}
